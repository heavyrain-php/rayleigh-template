description: Invalid request error
content:
  application/json:
    schema:
      type: object
      properties:
        ok:
          type: boolean
          description: OK
          example: false
      required:
        - ok
